import matplotlib.pyplot as plt
a = [1,2,3,4,5,6,5,4,3,2,1]
plt.plot(a)

[10,20,30,40,50,60,50,40,30,20,10]
plt.plot(a,b)

a = [1,2,3,4,5,6,5,4,3,2,1]
b = [10,20,30,40,50,60,50,40,30,20,10]
plt.plot(a,b)
plt.xlabel('year')
plt.ylabel('Yield(tones per hector)')

from matplotlib import pyplot as plt
#ploting our canvas
plt.plot([1,2,3],[4,5,1])
#display the graph
plt.show()

x = [1,2,3]
y = [10,11,12]
plt.plot(x,y)
plt.title("Line graph")
plt.ylabel('Y axis')
plt.xlabel('X axis')


import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# Load dataset
heart = pd.read_csv("heart disease.csv")

# Print column names
print(heart.columns)

# Create target column
if 'num' in heart.columns:
    heart['target'] = heart['num'].apply(lambda x: 0 if x == 0 else 1)

sns.set_style("whitegrid")

# Heatmap
plt.figure(figsize=(10,6))
sns.heatmap(heart.corr(numeric_only=True), annot=True, cmap='coolwarm')
plt.title("Correlation Heatmap")
plt.show()

# Countplot
if 'target' in heart.columns:
    sns.countplot(x='target', data=heart)
    plt.title("Heart Disease Count")
    plt.xlabel("0 = No Disease , 1 = Disease")
    plt.ylabel("Count")
    plt.show()

# Age Distribution
if 'age' in heart.columns:
    plt.figure(figsize=(8,5))
    sns.histplot(heart['age'], bins=20, kde=True)
    plt.title("Age Distribution")
    plt.xlabel("Age")
    plt.ylabel("Frequency")
    plt.show()

# Chest Pain vs Disease
if 'cp' in heart.columns and 'target' in heart.columns:
    sns.countplot(x='cp', hue='target', data=heart)
    plt.title("Chest Pain vs Heart Disease")
    plt.show()

# Pairplot
pair_columns = ['age', 'trestbps', 'chol']



sns.pairplot(heart, hue='target', vars=pair_columns)
plt.show()
